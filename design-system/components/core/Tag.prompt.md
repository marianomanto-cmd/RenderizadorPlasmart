A small mono pill for specs and metadata. `outline` is the default hairline chip; `accent` tints indigo; `solid` is a filled near-white chip.

```jsx
<Tag>Láser</Tag>
<Tag variant="accent">Hasta 32 mm</Tag>
<Tag variant="solid">Nuevo</Tag>
```
