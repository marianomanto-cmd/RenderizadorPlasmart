The "Capacidades" list row: a mono index, a giant thin name that lifts from muted to white, a description that fades in on hover and stays, and a mono spec on the right. The row indents and grows an indigo tick on hover. Stack several inside a flex column with hairline dividers.

```jsx
<CapabilityRow index="01" name="Corte láser" spec="↳ hasta 12,7 mm"
  description="Tecnología avanzada para cortes finos y detallados en acero." />
```
